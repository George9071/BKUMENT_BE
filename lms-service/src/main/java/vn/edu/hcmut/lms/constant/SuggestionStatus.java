package vn.edu.hcmut.lms.constant;

/**
 * The lifecycle of a proposal:
 * PENDING  — awaiting admin/moderator processing.
 * APPROVED — approved and the corresponding subject/topic has been created.
 * REJECTED — rejected, with reason.
 */
public enum SuggestionStatus { PENDING, APPROVED, REJECTED }

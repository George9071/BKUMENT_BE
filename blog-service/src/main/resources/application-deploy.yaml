spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/bkument_db
    driverClassName: org.postgresql.Driver
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

minio:
  endpoint: http://${MINIO_HOST}:9000
  access-key: ${MINIO_ACCESS_KEY}
  secret-key: ${MINIO_SECRET_KEY}
  bucket-name: ${MINIO_BUCKET_NAME}
  secure: false

gateway:
  base-url: ${BACKEND_HOST}
  api-prefix: /api/v1

swagger:
<<<<<<< Updated upstream
  gateway-url: ${BACKEND_HOST}/blog
  host-url: ${BACKEND_HOST}/blog
=======
  gateway-url: ${BACKEND_HOST}/api/v1/blog
>>>>>>> Stashed changes

app:
  services:
    profile: http://profile-service:8081/profile
    social: http://social-service:8089/social

  trending:
    w1: 100.0
    w2: 1.0
    w3: 5.0
    gravity: 1.8
server:
  port: 8081
  servlet:
    context-path: /profile

spring:
  neo4j:
    uri: bolt://${NEO4J:localhost}:7687
    authentication:
      username: ${NEO4J_USERNAME:username}
      password: ${NEO4J_PASSWORD:password}
  datasource:
    url: "jdbc:postgresql://${POSTGRESQL:localhost}:5432/bkument_db"
    driverClassName: "org.postgresql.Driver"
    username: ${POSTGRES_USERNAME:username}
    password: ${POSTGRES_PASSWORD:password}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9094
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

swagger:
  host-url: http://localhost:8081/profile

jwt:
  signerKey: "5016f0096d30f88524edc817d00b36406e26788c87ea9c97d57c4b7758b5f033"